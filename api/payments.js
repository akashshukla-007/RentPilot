const {supabaseAdmin}=require("../lib/supabase");const {requireUser}=require("../lib/auth");
module.exports=async(req,res)=>{const u=await requireUser(req,res);if(!u)return;if(req.method==="GET"){const {data,error}=await supabaseAdmin.from("payments").select("*").eq("owner_id",u.id);if(error)return res.status(500).json({error:error.message});return res.json(data)}if(req.method==="POST"){const b=req.body||{};const {data,error}=await supabaseAdmin.rpc("record_payment",{p_owner_id:u.id,p_flat_id:b.flat_id,p_tenant_id:b.tenant_id||null,p_bill_id:b.bill_id||null,p_amount:Number(b.amount||0),p_payment_date:b.payment_date||new Date().toISOString().slice(0,10),p_mode:b.mode||"cash",p_reference:b.reference||null,p_notes:b.notes||null});if(error)return res.status(400).json({error:error.message});return res.status(201).json(data)}res.status(405).json({error:"Method not allowed"})};