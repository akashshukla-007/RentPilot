const {supabaseAdmin}=require("../lib/supabase");const {requireUser}=require("../lib/auth");
module.exports=async(req,res)=>{const u=await requireUser(req,res);if(!u)return;if(req.method==="GET"){const {data,error}=await supabaseAdmin.from("tenants").select("*").eq("owner_id",u.id);if(error)return res.status(500).json({error:error.message});return res.json(data)}if(req.method==="POST"){const b=req.body||{};const {data,error}=await supabaseAdmin.from("tenants").insert({owner_id:u.id,flat_id:b.flat_id,name:b.name,mobile:b.mobile,email:b.email||null,move_in:b.move_in||null,whatsapp_opt_in:Boolean(b.whatsapp_opt_in),status:"active"}).select().single();if(error)return res.status(400).json({error:error.message});await supabaseAdmin.from("flats").update({status:"occupied"}).eq("id",b.flat_id).eq("owner_id",u.id);return res.status(201).json(data)}res.status(405).json({error:"Method not allowed"})};