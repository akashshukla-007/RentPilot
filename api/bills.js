const {supabaseAdmin}=require("../lib/supabase");const {requireUser}=require("../lib/auth");
module.exports=async(req,res)=>{const u=await requireUser(req,res);if(!u)return;if(req.method==="GET"){const {data,error}=await supabaseAdmin.from("bills").select("*").eq("owner_id",u.id);if(error)return res.status(500).json({error:error.message});return res.json(data)}if(req.method==="POST"){const b=req.body||{};const {data,error}=await supabaseAdmin.rpc("create_monthly_bill",{p_owner_id:u.id,p_flat_id:b.flat_id,p_month:b.month,p_current_reading:Number(b.current_reading||0)});if(error)return res.status(400).json({error:error.message});return res.status(201).json(data)}res.status(405).json({error:"Method not allowed"})};