const {supabaseAdmin}=require("./supabase");
async function requireUser(req,res){const h=req.headers.authorization||"";const t=h.startsWith("Bearer ")?h.slice(7):null;if(!t){res.status(401).json({error:"Login required"});return null}const {data,error}=await supabaseAdmin.auth.getUser(t);if(error||!data.user){res.status(401).json({error:"Invalid session"});return null}return data.user}
module.exports={requireUser};